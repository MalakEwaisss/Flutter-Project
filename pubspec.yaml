name: flutter_application_1
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.12.0
  flutter_map: 6.1.0
  latlong2: ^0.9.0
  http: ^1.1.0

# After:
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0 # Updated to the latest major version

flutter:
  uses-material-design: true
